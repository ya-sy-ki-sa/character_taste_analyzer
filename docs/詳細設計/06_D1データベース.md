# DD-06 現行D1データベース

## 1. 正本とbaseline

D1は認証、Entry、履歴、根拠、Profile、生成、Jobの正本である。migrationは[001_initial.sql](database/001_initial.sql)と[002_seed_ontology_v1.sql](database/002_seed_ontology_v1.sql)の2件へsquashし、空DBから46テーブルを構築する。旧migrationへ追記して互換状態を再現しない。

## 2. 46テーブル

| group | tables |
|---|---|
| auth / usage | `users`, `credentials`, `sessions`, `request_rate_limits`, `usage_daily` |
| catalog | `works`, `character_identities`, `character_representations` |
| ontology | `attribute_schema_versions`, `attribute_definitions` |
| source | `sources`, `source_sets`, `source_set_items` |
| entry | `user_character_entries`, `entry_revisions` |
| understanding | `model_run_metadata`, `character_understanding_runs`, `character_understanding_snapshots`, `raw_attribute_mentions`, `attribute_mappings`, `character_assertions`, `customization_deltas`, `understanding_reviews` |
| preference / evidence | `analysis_runs`, `preference_assertions`, `value_stance_assertions`, `evidence_fragments` |
| profile | `profile_projections`, `profile_dimensions`, `profile_snapshots`, `profile_snapshot_items` |
| graph | `graph_projection_snapshots`, `graph_projection_nodes`, `graph_projection_edges` |
| generation | `generation_requests`, `generation_request_preferences`, `generation_briefs`, `generated_characters`, `generation_basis_links`, `generation_validation_runs` |
| async / export | `quota_reservations`, `jobs`, `job_attempts`, `outbox_events`, `projection_rebuild_states`, `account_exports` |

## 3. 統合規則

- source本文、hash、出典metadataは`source_documents`等へ分割せず`sources` 1行に保持する。
- source setはversion tableを作らず、set自体を不変単位としてitemと結ぶ。
- workの現行metadataは`works`へ保持する。
- generated characterのJSON、hash、model metadata、brief参照は`generated_characters`へ保持する。
- Entry作成冪等性は`creation_fingerprint`で判定し、draft payloadを重複保存しない。
- credentialは`user_id`を主キーとして1ユーザー1件に限定する。

## 4. 維持する履歴

実際に複数世代を持つ次の履歴は維持する。

- Entry revision
- understanding / analysis runとsnapshot
- review
- generation briefとbasis / validation
- ProfileSnapshotと選択item
- Job attempt

Profileのlive projectionは再構築可能である。一方、生成が参照したProfileSnapshotは生成成果物の根拠として不変に保持し、live profileの再構築や削除済みEntryの状態から独立させる。

## 5. 削除した構造

旧Entry、旧根拠、固定値だけを持つversion／visibility／soft-delete列、予約だけで書込経路のないtable、1対1の冗長revision tableはbaselineに含めない。特に次の名前がDDLへ戻ることを契約検査で禁止する。

- `source_documents`, `source_document_revisions`, `source_fragments`
- `source_set_versions`, `work_versions`, `generated_character_revisions`
- consent、asset、relation、correction、similarity、feedback、pattern系table
- `idempotency_responses`, `attribute_aliases`, `preference_value_stance_links`

Evidenceの`verification_status`は`verified_quote`、`source_attributed`、`model_knowledge`、`invalid`だけであり、`legacy_unverified`は許可しない。

## 6. 整合性と削除

全所有データは`owner_user_id`または親FKで所有者を解決する。外部キーを有効にし、account削除はcredential、sessionを含む所有データをcascadeで物理削除する。現行機能に書込経路のないsoft-delete flagは置かない。

外部LLM、R2、Workflow APIはD1 transaction中に呼ばない。外部結果は短いtransactionで保存し、非同期状態は`jobs`、`job_attempts`、`outbox_events`で追跡する。

## 7. 検査

空DBに2 migrationを適用し、table数46、FK、index、ontology seedを検査する。移行時は件数とcontent hashを比較し、`PRAGMA foreign_key_check`が0件、`PRAGMA integrity_check`が`ok`であることをbinding切替条件とする。
