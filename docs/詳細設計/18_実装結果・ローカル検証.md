# DD-18 現行baseline実装結果・ローカル検証

## 1. 実装結果

2026-08-30に、互換性より現行機能と現行成果物を優先する破壊的refactoringを実施した。

- Entryを現行3 variantへ一本化し、v1 fieldとfallbackを削除
- key rotation API、credential/session generationを削除
- Evidenceを現行4状態へ限定
- Profileの空`patterns`と関連graph種別を削除
- generated character IDを成果物の単一識別子へ統一
- DataStore Strategy Port／FactoryとD1 Adapterを分離
- LLM Provider／Fallback、Embedding Provider／Factoryを維持
- `DEPLOYMENT_PROFILE`と未実装Vectorize binding/readinessを削除
- 6 migration・66 tableを2 baseline・46 tableへsquash

## 2. side-by-side移行

旧ローカルD1とは別の`character-taste-lab-current-local`を作成し、現行成果物だけを移行した。

| 対象 | 移行結果 |
|---|---:|
| users | 42 |
| 現行credentials | 42 |
| 未期限切れsessions | 40 |
| Entries | 27 |
| v2 Entry revisions | 41 |
| 現行evidence | 723 |
| generated characters | 13 |
| 生成根拠用の不変ProfileSnapshots | 13 |
| 再開用`waiting_for_user` Jobs | 18 |

v1 revision 23件、`legacy_unverified` evidence 432件、旧live profile／graph projection、完了済みJob／attempt／outbox／export履歴は移行していない。active Entry所有者のprofile／graphは現行データから再構築対象とした。

## 3. 検証結果

- credential digest、未期限切れsession、Entry status／active revisionの移行前後差分: 0
- generated character JSON／content hashの差分: 0
- `PRAGMA foreign_key_check`: 0件
- `PRAGMA integrity_check`: `ok`
- baseline DDL: 2 migration、46 tables、ontology 94 attributes
- unit／contract: 15 files、104 tests成功
- Chromium E2E: 7 tests成功

WebKit E2Eは実行環境のOS library不足でbrowser processを起動できないため未完了である。製品flowは同じfixtureのChromiumで全件成功している。

## 4. 旧資産

検証後、対象pathを個別に確認して旧ローカルD1 2件、backup 4件、移行専用script／report、旧migration、`wrangler.jsonc.org`を削除した。削除した旧DBとbackupはworkspace内からは復元できない。

staging／production D1の作成、remote migration、binding切替、deployは実施していない。

## 5. 再検証

```bash
npm run typecheck
npm test
npm run contracts
npm run build
npx playwright test --project=chromium
```

DB契約は空DBへbaselineを適用して検証する。既存DBへ旧migration列を足し戻す運用は行わない。
