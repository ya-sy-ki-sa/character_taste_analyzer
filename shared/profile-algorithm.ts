/** Persisted algorithm identifier and its UI label share the same source. */
export const PROFILE_ALGORITHM_VERSION = "profile/v1.2.0-domain-aware";
export const PROFILE_ALGORITHM_LABEL = PROFILE_ALGORITHM_VERSION.split("/")[1].split("-")[0];
