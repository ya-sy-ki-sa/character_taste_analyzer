# Codexの作業指示

## 探索と正本

- 作業開始時に `git status --short` と関連差分を確認し、ユーザーの既存変更を保護する。
- Node.js 24とnpmを使う。必要に応じて[構成と正本](docs/architecture.md)、[導入・運用](docs/operations.md)、[品質評価](docs/quality.md)を読む。
- 生成物は直接編集せず、正本を変更して `npm run assets:generate` で再生成する。依存ライセンスは `npm run licenses:generate` を使う。archiveは履歴参照に限定する。

## 実装と判断

- 依頼された作業は調査・実装・検証・結果報告まで進める。計画や読み取りだけの依頼では、その範囲を守る。
- 通常の探索や、依頼の範囲内の修正で承認を繰り返さない。結果を左右する不明点だけ質問し、回答を待つ間も独立した作業を続ける。
- 上位指示の範囲で、ユーザーの明示指示をSkillの一般的な指針より優先する。公開、デプロイ、remote DB操作、他者への送信は依頼の承認範囲を確認し、未承認ならレビュー可能な結果を準備してから確認する。
- 初版は単一エージェントで作業する。サブエージェントへの委任はユーザーが明示的に求めた場合に限る。

## Skill

- 通常版のUI作業には[通常版デザインSkill](.agents/skills/character-taste-observatory-design/SKILL.md)を使う。dark版のUI作業には[dark版デザインSkill](.agents/skills/character-taste-dark-observatory-design/SKILL.md)と通常版の共通基盤を使う。
- Skillが停止や確認の根拠になった場合は、読んだファイルへのリンクと該当指示を示し、明示的な要件と自身の解釈を区別する。

## 検証と報告

- 文書だけの変更はリンク・手順・差分を確認する。設定変更は構文と実際の読み込みも確認する。
- コード変更は関連テストと型検査を行い、引き渡し前に `npm run verify` を1回通す。失敗・追加変更・未解決の懸念がある場合に必要な検証を再実行し、成功済みの検証を理由なく反復しない。
- UI変更は対応するSkillに従い、通常版・dark版への影響とPlaywrightの画面結果を確認する。
- ローカル検証は既存のoffline／Fake／Replayと専用E2E環境を使う。アプリの実LLM評価は、明示的に依頼された場合に実施する。
- モデル利用不可・設定不整合・検証環境不足では原因と残作業を報告し、別モデルへ自動で切り替えない。
- 最終報告は変更内容、実行した検証と結果、未解決事項を簡潔な日本語で示す。未実行・失敗・スキップを成功扱いしない。

依頼テンプレート、再開手順、導入時の確認ケースは[Codex開発ハーネス](docs/codex-harness.md)を参照する。
